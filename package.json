{
  "name": "3d-packing-simulator",
  "version": "2.4.0",
  "private": true,
  "description": "Client-side 3D bin packing simulator and visualizer",
  "scripts": {
    "check": "node --check packer.js && node --check app.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
